# Ghidra Decompiler API Inventory (Pinned)

Source root:
- `third_party/ghidra/Ghidra/Features/Decompiler/src/decompile/cpp`

Pinned upstream for this inventory:
- Tag: `Ghidra_12.0.3_build`
- Commit: `09f14c92d3da6e5d5f6b7dea115409719db3cce1`
- Commit date: `2026-02-10`

## Inventory Table

| Symbol name | File path | Kind | Inputs/outputs | Ownership/lifetime | Required initialization order | Error propagation mechanism | Thread-safety notes | MVP relevance | Pin reference |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `startDecompilerLibrary(const char*, const vector<string>&)` | `libdecomp.cc` | function | In: root path + extra spec paths. Out: initialized global registries/spec paths. | Process-global setup; no object return. | Must run before any `Architecture` construction/decompile. | Throws `LowlevelError` from deeper setup paths if called later with bad config assumptions. | Uses global static registries/capability lists; serialize startup. | `required` | `Ghidra_12.0.3_build` |
| `ArchitectureCapability::findCapability` | `architecture.hh` | function | In: binary path or XML doc. Out: architecture capability implementation. | Returns pointer owned by static capability registry. | Requires `CapabilityPoint::initializeAll()` and sorted capabilities. | Null return for unknown format; callers must map to structured error. | Shared global capability list. | `required` | `Ghidra_12.0.3_build` |
| `Architecture::init(DocumentStorage&)` | `architecture.cc` | function | In: pre-created `DocumentStorage`. Out: fully initialized decompiler subsystems. | `Architecture` owns loader, translator, type/comment/context DBs, symbol DB, print/action objects. | Mandatory order in implementation: loader -> resolve arch -> spec file -> context/type/comment/string/constant/database -> restore spec -> core types -> symbols -> instructions -> readonly ranges. | Throws `DecoderError`, `LowlevelError`, `SleighError` from subsystems. | Not safe for concurrent mutation on same instance. | `required` | `Ghidra_12.0.3_build` |
| `Architecture::clearAnalysis(Funcdata*)` | `architecture.cc` | function | In: function object. Out: cleared per-function analysis state. | Caller owns `Funcdata`; architecture clears analysis artifacts in-place. | Only valid after function has been started/processed. | Throws `LowlevelError` on inconsistent function state. | Function-local; no global lock protection. | `required` | `Ghidra_12.0.3_build` |
| `SleighArchitecture::scanForSleighDirectories` | `sleigh_arch.cc` | function | In: Ghidra-like root dir. Out: `specpaths` populated. | Updates static `specpaths`. | Must happen before language enumeration or architecture `buildSpecFile`. | Missing dirs do not throw immediately; later lookup fails in `buildSpecFile`. | Writes global path list; serialize. | `required` | `Ghidra_12.0.3_build` |
| `SleighArchitecture::getDescriptions` | `sleigh_arch.cc` | function | Out: vector of `LanguageDescription` parsed from `.ldefs`. | Returns static vector reference. | Requires populated `specpaths` and readable `.ldefs`. | Throws `LowlevelError` for parse/load problems. | Shared static cache; read-only after load, but initialization race risk. | `required` | `Ghidra_12.0.3_build` |
| `SleighArchitecture::resolveArchitecture` | `sleigh_arch.cc` | function | In: `archid`/target/loader arch type. Out: selected language index. | Mutates architecture instance fields (`archid`, `languageindex`). | Requires loader ready and language descriptions loaded. | Throws `LowlevelError` if no matching language id. | Instance-local, but depends on static description cache. | `required` | `Ghidra_12.0.3_build` |
| `SleighArchitecture::buildSpecFile` | `sleigh_arch.cc` | function | In: selected language + compiler tag. Out: `.pspec`, `.cspec`, `.sla` loaded into `DocumentStorage`. | Uses external files; store owns parsed documents for init pass. | Must run after `resolveArchitecture`, before translator/type/context restore. | Throws `SleighError`/`LowlevelError` for missing or invalid spec files. | Reads static `specpaths`; avoid concurrent path mutation. | `required` | `Ghidra_12.0.3_build` |
| `LanguageDescription::getCompiler` | `sleigh_arch.cc` | function | In: compiler id string. Out: matching `CompilerTag` or fallback. | Returned reference owned by `LanguageDescription`. | Used inside `buildSpecFile`. | No exception for unknown id; falls back to `default`/first compiler entry. | Read-only data. | `required` | `Ghidra_12.0.3_build` |
| `IfaceDecompData::followFlow` | `ifacedecomp.cc` | function | In: selected `Funcdata`, optional size bound. Out: initial CFG + pcode, console diagnostics. | Console layer uses currently selected function pointer; no transfer of ownership. | Requires `conf` and `fd` already set by load commands. | Catches `RecovError` and prints message instead of hard-failing. | Console not designed for concurrent sessions in same object. | `optional` | `Ghidra_12.0.3_build` |
| `Funcdata` | `funcdata.hh` | class | In: function symbol/scope/address/size. Out: CFG, pcode, SSA, warnings, prototype state. | Owned by symbol table (`FunctionSymbol`) and cleared/reused by architecture. | Must call `startProcessing`/`followFlow` before action pipeline. | Internal methods throw `LowlevelError`/`RecovError` variants; warnings can be attached. | Per-function mutable state, not thread-safe shared. | `required` | `Ghidra_12.0.3_build` |
| `Funcdata::followFlow` | `funcdata.hh` / `funcdata.cc` | function | In: begin/end address bounds. Out: discovered basic blocks and pcode ops. | Mutates `Funcdata` internals in-place. | Called before major optimization actions. | Can throw recoverable/low-level analysis errors; some issues converted to warnings. | Not thread-safe on same object. | `required` | `Ghidra_12.0.3_build` |
| `Funcdata::warning` / `warningHeader` | `funcdata.hh` / `funcdata.cc` | function | In: warning text + address/header context. Out: warning comment records in comment DB. | Writes into active `CommentDatabase` linked to architecture. | Available during/after analysis actions. | Does not throw on normal paths; warning channel for degraded analysis. | Shared comment DB if shared architecture. | `required` | `Ghidra_12.0.3_build` |
| `ActionDatabase::universalAction` | `coreaction.cc` | function | In: architecture config. Out: default root action graph (`decompile` and groups/rules). | `Architecture::allacts` owns cloned action groups/rules. | Must run before invoking `allacts.getCurrent()->perform`. | Rule execution may throw lower-level decompile errors through actions. | Action graph mutable; no built-in concurrent guards. | `required` | `Ghidra_12.0.3_build` |
| `Action::reset` / `Action::perform` (via current root action) | `action.hh` / `ifacedecomp.cc` call path | function | In: `Funcdata`. Out: optimized/decompiled function state. | No ownership transfer; operates on caller-owned `Funcdata`. | `reset` then `perform` order is required for each invocation. | Returns status (`<0` break), may throw decompiler errors. | Same action object should not be shared concurrently without synchronization. | `required` | `Ghidra_12.0.3_build` |
| `TypeFactory` | `type.hh` | class | In: parsed types/spec data. Out: canonicalized datatype graph used by analysis/printer. | Owned by architecture (`types` pointer). | Built before restoring spec and before print/type-dependent actions. | Type parsing/build can throw `LowlevelError`. | Mutable caches; context-local use recommended. | `required` | `Ghidra_12.0.3_build` |
| `TypeFactory::setCoreType` / `cacheCoreTypes` | `type.hh` / `type.cc` | function | In: primitive type definitions. Out: baseline type system entries. | Stored in architecture-owned `TypeFactory`. | Called during `buildCoreTypes` phase after spec restore. | Throws on malformed definitions/conflicts. | Not synchronized globally. | `required` | `Ghidra_12.0.3_build` |
| `LoadImage::loadFill` | `loadimage.hh` | function (virtual) | In: addr + size. Out: bytes copied into caller buffer. | Concrete loader owns backend handle; caller owns output buffer. | Loader must be built before resolve/spec/decompile. | Throws `DataUnavailError`/`LowlevelError` for unreadable regions. | Backend-specific; often not thread-safe without external lock. | `required` | `Ghidra_12.0.3_build` |
| `RawLoadImage` | `loadimage.hh` / `loadimage.cc` | class | In: raw file path + space attachment. Out: byte provider for raw binaries. | `Architecture` owns loader; file handle managed by loader lifetime. | Used when raw capability selected. | Throws on file open/read/seek failures. | No internal locking. | `optional` | `Ghidra_12.0.3_build` |
| `ParserContext` | `context.hh` | class | In: instruction bytes + context words. Out: parser/decode state used by SLEIGH translation. | Allocated and managed per decode flow; not a global singleton. | Translator/context systems must be built before instruction decode. | Parsing/setup errors surface as `SleighError`/`LowlevelError`. | Mutable decode state, not safe for concurrent mutation. | `required` | `Ghidra_12.0.3_build` |
| `ContextDatabase` | `globalcontext.hh` (built by `buildContext` in `architecture.cc`) | class | In: context register settings by address/range. Out: decode context for translator/flow. | Owned by architecture (`context`). | Must exist before flow decode and optional context overrides. | Throws for unknown variable names/range errors. | Mutable state; synchronize writes if shared. | `required` | `Ghidra_12.0.3_build` |
| `Comment` + `Comment::comment_type` | `comment.hh` | class + enum | In: comment type/function addr/inst addr/text. Out: sortable warning/comment items. | Stored in `CommentDatabase` implementation. | Used during decompile and print emission. | Encoding/decoding methods can throw on malformed streams. | Data structure itself is not synchronized. | `required` | `Ghidra_12.0.3_build` |
| `CommentDatabase` / `CommentDatabaseInternal` | `comment.hh` | class | In: comment insert/query requests. Out: iterators over function comments. | Architecture-owned DB; comment objects owned by DB. | Must be initialized before decompile actions producing warnings/comments. | Virtual interface; backend may throw on decode/serialization issues. | Not thread-safe by default. | `required` | `Ghidra_12.0.3_build` |
| `Emit` / `EmitMarkup` / `EmitPrettyPrint` | `prettyprint.hh` / `prettyprint.cc` | class | In: tagged print events from `PrintLanguage`. Out: formatted stream output (plain/XML-like). | Print language owns emitter; output stream owned by caller. | Print subsystem initialized after architecture/spec restoration. | Formatting layer generally non-throwing; upstream print can throw. | Mutable formatter state per printer instance. | `optional` | `Ghidra_12.0.3_build` |
| `PrintLanguage::docFunction` | `printlanguage.hh` / `printlanguage.cc` | function | In: decompiled `Funcdata`. Out: rendered C (or other language) text. | Uses architecture-owned printer and caller-provided stream. | Requires completed or partial decompile state + selected print language. | May throw if print state/data is inconsistent. | Printer instance is mutable; single-thread use per context. | `required` | `Ghidra_12.0.3_build` |
