# API Reference

The API reference is auto-generated from source code docstrings and type
annotations. It covers every public symbol exported from the `flatline` package.

Browse the reference by topic:

- **[API Reference](api.md)** — the full public API: session lifecycle,
  one-shot wrappers, request/result models, data models, diagnostics,
  enumeration types, and public constants.
- **[Error Taxonomy](errors.md)** — the complete error hierarchy, per-category
  descriptions, and the `ERROR_CATEGORIES` / `CATEGORY_TO_EXCEPTION` mapping.
